# I deployed Exchange server with only one server and one Active Directory.
# used Azure cloud solution and create 2 machines windows server based withithn the same resource group.
# peered both machine's subnet using static private IPs from azure network settings.
